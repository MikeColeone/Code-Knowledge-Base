# TCP 和 UDP 的区别

## 核心答案

TCP 面向连接、可靠、有序，适合对正确性要求高的场景；UDP 无连接、开销小、延迟低，适合实时性优先的场景。

## 对比

- TCP 有确认、重传、流量控制、拥塞控制。
- UDP 不保证到达、不保证顺序，也没有重传机制。
- TCP 更适合 HTTP、文件传输、数据库连接。
- UDP 更适合音视频、直播、实时游戏、DNS 查询。

## 前端面试切入点

面试里常会把这个问题和 WebSocket、HTTP/3、直播连麦、弱网体验一起问。
