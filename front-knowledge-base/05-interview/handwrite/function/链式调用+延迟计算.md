function sum(...args) {
  function next(...innerArgs) {
    args.push(...innerArgs);
    return next;
  }
  next.valueOf = next.toString = () => {
    return args.reduce((r, c) => r + c, 0);
  };

  return next;
}
