# 数组和链表的区别

## 核心答案

数组适合随机访问，链表适合频繁插入删除。数组的元素通常连续存储，链表节点通过指针连接。

## 对比

- 数组按下标访问快，通常是 `O(1)`。
- 链表查找慢，通常要顺序遍历。
- 数组中间插入删除成本高。
- 链表插入删除更灵活，但额外占用指针空间。

## 前端面试切入点

如果题目要求高频头尾插入、LRU、消息队列、任务调度，通常会延伸到链表或双向链表。
