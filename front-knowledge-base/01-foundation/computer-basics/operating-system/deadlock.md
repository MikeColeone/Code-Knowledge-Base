# 死锁是什么，如何避免

## 核心答案

死锁是多个执行单元彼此等待对方持有的资源，导致谁都无法继续推进。它通常和资源竞争、加锁顺序不一致有关。

## 四个必要条件

- 互斥
- 请求并保持
- 不可剥夺
- 循环等待

## 常见避免方式

- 统一加锁顺序
- 缩小锁粒度
- 设置超时和重试
- 尽量减少多个资源同时持有

## 前端面试切入点

虽然前端较少直接写多线程锁，但在 Node、数据库事务、分布式任务协作里经常会被问到。
