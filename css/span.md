1. span由于是内联元素，所以设置宽高无效，当设置 `displya:inline-block;`时生效；
2. span设置 `margin-left,margin-right`是可以的，`margin-top,margin-bottom`无效；
3. span设置 `padding-left,padding-right`是可以的，`padding-top,padding-bottom`无效；
