通过requestAnimationFrame，实现一个迭代器

题目描述

const [start, stop] = fn(callback, 1000);

// start(); // 之后,每隔1s, 执行一下那个回调，

// stop(); // 之后，停止该任务

// strat(); 有可能被dom事件重复调用，但是，要保证 回调依然是每隔1s执行

测试代码

start();

setTimeout(() =>{

    stop();

}, 5000)
